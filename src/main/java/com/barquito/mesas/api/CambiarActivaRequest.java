package com.barquito.mesas.api;

/**
 * DTO de request para activar/desactivar una mesa.
 *
 * <p>Stub — implementación real pendiente (Fase 6).
 *
 * @param activa nuevo valor de habilitación.
 */
public record CambiarActivaRequest(boolean activa) {}
